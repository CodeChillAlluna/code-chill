window.env = {
  /**
  	The url where your Code&Chill server is running
    (In our case it's the same machine as the Code&Chill client, so localhost will do it)
  **/
  restApi: "http://localhost:8080",
  /**
  	The url where your Code&Chill server is running for letting user access their IDE
    (In our case it's the same machine as the Code&Chill client, so localhost will do it)
  **/
  docker: "http://localhost"
};